
# starting docker with splash server
sudo docker run -p 8051:8051 scrapinghub/splash &

# go to feedbot root and start scrapyd
scrapyd
#the config is in scrapy.cfg

# start django server

# filename is only updated on refreshing the whole site
